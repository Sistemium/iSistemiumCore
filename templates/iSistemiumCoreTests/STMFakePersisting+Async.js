const {meta, methods} = require('../STMFakePersisting');

const file = {
    className: 'STMFakePersisting',
    categoryName: 'Async',
    queueConst: 'STM_FAKE_PERSISTING'
};

module.exports = {methods, meta, file};